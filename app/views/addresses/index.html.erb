<p style="color: green"><%= notice %></p>

<h1>Addresses</h1>

<div id="addresses">
  <% @addresses.each do |address| %>
    <%= render address %>
    <p>
      <%= link_to "Show this address", paciente_address_path(@address.paciente_id) %>
    </p>
  <% end %>
</div>

<%= link_to "New address", new_paciente_address_path %>
